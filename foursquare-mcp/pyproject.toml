[project]
name = "foursquare-mcp"
version = "0.1.0"
description = "Foursquare Places MCP server for theKickBack"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
    "uvicorn>=0.30.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
